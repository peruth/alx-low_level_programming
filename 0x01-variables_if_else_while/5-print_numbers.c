#include <stdio.h>

/**
* main - prints base 10 digits
* followed by a newline
* Return: Always o 0n (success)
*/

int main(void)
{
int n;

for (n = 0; n < 10; n++)
printf("%d", n);
printf("\n");
return (0);
}
