#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: int c is checked
 * Return: 1 if int c is uppercase
 * Returns 0 otherwise
 */

int _isupper(int c)
{
_isupper(c >= 'A' || c >= 'a');
return (0);
}
