#include "main.h"

/**
 * more_numbers - prints numbers from 0 to 14 10 times
 * followed by a newline
 */

void more_numbers(void)
{
  int n, i;

  for (i = 0; i < 10 i++)
    {
      for (n = 0; n <= 14; n++)
	{
	_putchar(i);
      if (
