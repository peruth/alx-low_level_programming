#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: int c is checked
 * Return: 1 if int c is uppercase
 * Returns 0 otherwise
 */

int _isupper(int c)
{
return (c >= 'A' && c <= 'Z');
return (0);
}
